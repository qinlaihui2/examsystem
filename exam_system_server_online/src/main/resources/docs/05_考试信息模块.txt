 //开始考试
        //宏观： 创建一个考试对象，并存储到数据库即可
        //1. 校验，该学生当前试卷是否存在正在考试的记录！ 存在进行中，返回即可
        //2. 创建新的考试记录！赋予传入的参数（学生姓名，试卷id） 补全（状态，时间，切屏数）
        //3. 保存即可！
        //4. 返回即可


 1,提交考试业务接口
   1.1将集合转换成answeRecord对象集合
   1.2进行answerRecord集合的批量保存
   1.3修改考试记录对象（状态修改成已完成 2.endtime时间）
 2.定义判卷业务的接口
  examId->获取学生对应的答题记录获取试卷信息
  对比考试记录的答案判断
  同时进行分数的累加和-
  没有使用ai所以只能判断选择题和判断题
